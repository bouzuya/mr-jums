import * as assert from 'power-assert';
import beater from 'beater';

import { findEntry } from '../../../src/model/entry-viewer/find-entry';

const { test } = beater();

const category = 'model > entry-viewer > find-entry > ';

test(category + '*', () => {
  // TODO
  assert(findEntry);
});
