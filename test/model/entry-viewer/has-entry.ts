import * as assert from 'power-assert';
import beater from 'beater';

import { hasEntry } from '../../../src/model/entry-viewer/has-entry';

const { test } = beater();

const category = 'model > entry-viewer > has-entry > ';

test(category + '*', () => {
  // TODO
  assert(hasEntry);
});
