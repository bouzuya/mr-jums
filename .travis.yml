sudo: required
language: node_js
node_js:
- node
- '6'
- '5'
services:
  - docker
after_success:
  - yarn install --production
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login --email="${DOCKER_EMAIL}" --username="${DOCKER_EMAIL}" --password="${DOCKER_PASSWORD}" registry.heroku.com;
    docker build -t registry.heroku.com/mr-jums/web .;
    docker push registry.heroku.com/mr-jums/web;
    fi
